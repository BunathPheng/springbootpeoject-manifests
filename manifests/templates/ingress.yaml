---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: springbootpeoject                  # Ingress name
  labels:
    app: springbootpeoject
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "springbootpeoject.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: springbootpeoject               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
